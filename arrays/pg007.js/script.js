function longestWord(string){
    let maxLength = 0;
    let i = 0 ;
    let maxWordStart = -1
    let currentWordStart = -1
    console.log(maxWordStart, maxLength)
    console.log(string.slice(maxWordStart, maxWordStart+maxLength))
}

longestWord(" this longgggg ass")